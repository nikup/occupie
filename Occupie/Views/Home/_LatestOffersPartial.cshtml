@model IEnumerable<Occupie.Models.Offer>

@foreach (var offer in Model)
{
    <div class="normalize-offer-links">
        @Html.ActionLink(offer.Title, "Details", "Offer", new { id = offer.OfferId }, null)
    </div>
}